export class Person {
    firstName: string;
    initial: string;
    color: string;

    constructor(firstName: string, initial: string, color: string) {
        this.firstName = firstName;
        this.initial = initial;
        this.color = color;
    }
}
